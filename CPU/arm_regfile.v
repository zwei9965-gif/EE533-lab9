// arm_regfile.v ? ??? 64x32-bit ARM Register File
// ?????{thread_id[1:0], reg_addr[3:0]}
// ?????WB1 ???? + WB2 ?????
// Write-through bypass?????? WB ?? ID ?????ID ??????
`timescale 1ns/1ps
module arm_regfile (
    input  wire        clk,
    input  wire        rst,
    // ?? ID
    input  wire [1:0]  thread_id,   // ID ????????????
    input  wire [1:0]  wb_thread,   // WB ????????????
    // ?????? + write-through bypass?
    input  wire [3:0]  r0addr,
    output wire [31:0] r0data,
    input  wire [3:0]  r1addr,
    output wire [31:0] r1data,
    // ???1????? (DP/LDR ??)
    input  wire        we,
    input  wire [3:0]  waddr,
    input  wire [31:0] wdata,
    // ???2????? (pre/post-index Rn ??)
    input  wire        we2,
    input  wire [3:0]  waddr2,
    input  wire [31:0] wdata2
);
    // 64 ?????4 ?? × 16 ???
    reg [31:0] regs [0:63];
    integer i;

    // 6-bit ?????{thread_id[1:0], reg_addr[3:0]}
    wire [5:0] rd_addr0 = {thread_id, r0addr};
    wire [5:0] rd_addr1 = {thread_id, r1addr};
    wire [5:0] wr_addr  = {wb_thread, waddr};
    wire [5:0] wr_addr2 = {wb_thread, waddr2};

    // ?? ????write-through bypass??????????????????
    // ????WB2 > WB1 > ????
    assign r0data = (we2 && wr_addr2 == rd_addr0) ? wdata2 :
                    (we  && wr_addr  == rd_addr0) ? wdata  : regs[rd_addr0];
    assign r1data = (we2 && wr_addr2 == rd_addr1) ? wdata2 :
                    (we  && wr_addr  == rd_addr1) ? wdata  : regs[rd_addr1];

    // ?? ????????????????????????????????????????????
    always @(posedge clk) begin
        if (rst) begin
            for (i = 0; i < 64; i = i + 1)
                regs[i] <= 32'b0;
        end else begin
            if (we)
                regs[wr_addr]  <= wdata;
            if (we2)
                regs[wr_addr2] <= wdata2;
        end
    end
endmodule